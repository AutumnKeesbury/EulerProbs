public class main {
  // The arithmetic derivative is completely defined on the naturals by the
  // following properties:
  // 1. p' = 1 for any prime p;
  // 2. (ab)' = a'*b + b'*a for natural number a, b.
  //
  // For example, 24' = (2 * 12)' = 2'*(12) + (12)'*2 = 12 + (12)'*2,
  // but (12)' = (2 * 6)' = 2'*6 + 6'*2 = 6+ 6'*2,
  // and 6' = (2 * 3)' = 2'*3 + 3'*2 = 3+2 = 5.
  // Putting this all together, we get
  // (24)' = 12 + 2(6 + 2(5)) = 12 + 12 + 20 = 44.
  //
  // Find the sum of gcd(k,k') for 1<=k<= 5 * 10^15.

  public static void driver(String[] args) {
  }
}
